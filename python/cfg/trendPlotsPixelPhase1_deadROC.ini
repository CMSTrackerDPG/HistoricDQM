[plot:deadROC_BPix_L1]
metric = basic.BinCount(1,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC

[plot:deadROC_BPix_L2]
metric = basic.BinCount(2,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC

[plot:deadROC_BPix_L3]
metric = basic.BinCount(3,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC

[plot:deadROC_BPix_L4]
metric = basic.BinCount(4,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC

[plot:deadROC_FPix_R1]
metric = basic.BinCount(5,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC

[plot:deadROC_FPix_R2]
metric = basic.BinCount(6,True)
relativePath = PixelPhase1/deadRocTotal
runOffset = 132000
threshold = 0
yTitle = Number of Dead ROC